import { globalAtom, loadingAtom } from './atom';
import { useGlobal } from './useGlobal';
import { withGlobalState } from './withGlobal';

export { globalAtom, loadingAtom, withGlobalState };

export default useGlobal;
